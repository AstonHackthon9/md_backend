spring:

  jpa:
    show_sql: true
    defer-datasource-initialization: false
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.MySQLDialect"
        hbm2ddl:
          auto: "update"

  datasource:
    url: "jdbc:mysql://localhost:2000/md_db"
    username: "root"
    password: "123456789"

server:
  port: 8081